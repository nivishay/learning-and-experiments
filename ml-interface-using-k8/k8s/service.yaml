apiVersion: v1
kind: Service
metadata:
  name: ml-inference-service
spec:
  type: LoadBalancer
  selector:
    app: ml-inference       # matches the label on the Deployment's Pods
  ports:
    - protocol: TCP
      port: 80              # port the Service exposes externally
      targetPort: 8000      # port the container listens on
